  Open C:\Users\d1658\Documents\project\playgound\ini-diff-tool\index.html directly in Chrome or Edge (requires local file Web Worker support — Chrome with 
  --allow-file-access-from-files flag, or serve via npx serve .):

   cd C:\Users\d1658\Documents\project\playgound\ini-diff-tool
   npx serve .

  Then open http://localhost:3000 and:

   1. Drag your two .ini files into the LEFT/RIGHT drop zones
   2. The tool auto-parses and diffs — watch the progress overlay
   3. Click section triangles to expand/collapse sections  
   4. Check/uncheck sections in the sidebar to show/hide them
   5. Search sections with the filter box
   6. Click ↻ Watching to enable auto-refresh on file save